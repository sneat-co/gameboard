---
// Static, CSS-only scoreboard visual. No JS, no stock imagery.
---

<div class="scoreboard" role="img" aria-label="Live scoreboard showing Home 62, Away 58, clock 04:21 in the fourth quarter">
  <div class="board-top">
    <span class="live"><span class="pulse"></span>LIVE</span>
    <span class="period">Q4</span>
  </div>

  <div class="teams">
    <div class="team home">
      <span class="name">HOME</span>
      <span class="score">62</span>
    </div>

    <div class="clock">
      <span class="time">04:21</span>
      <span class="label">CLOCK</span>
    </div>

    <div class="team away">
      <span class="name">AWAY</span>
      <span class="score">58</span>
    </div>
  </div>
</div>

<style>
  .scoreboard {
    background: radial-gradient(120% 120% at 50% 0%, var(--gb-score-bg-top), var(--gb-score-bg));
    border: 1px solid var(--gb-score-bg-top);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    padding: var(--space-3);
    width: 100%;
    color: #fff;
  }

  .board-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-3);
  }

  .live {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    font-weight: 800;
    font-size: 0.8rem;
    letter-spacing: 0.14em;
    color: var(--color-danger);
  }

  .pulse {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--color-danger);
    box-shadow: 0 0 0 0 rgba(255, 92, 122, 0.6);
    animation: pulse 1.6s infinite;
  }

  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 rgba(255, 92, 122, 0.6);
    }
    70% {
      box-shadow: 0 0 0 12px rgba(255, 92, 122, 0);
    }
    100% {
      box-shadow: 0 0 0 0 rgba(255, 92, 122, 0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .pulse {
      animation: none;
    }
  }

  .period {
    font-weight: 800;
    letter-spacing: 0.1em;
    color: #9fb0bf;
  }

  .teams {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: var(--space-2);
  }

  .team {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
  }

  .name {
    font-weight: 700;
    letter-spacing: 0.18em;
    font-size: 0.85rem;
    color: #9fb0bf;
  }

  .score {
    font-family: var(--font-mono);
    font-weight: 800;
    font-size: clamp(3.5rem, 14vw, 6rem);
    line-height: 1;
    font-variant-numeric: tabular-nums;
  }

  .team.home .score { color: var(--gb-score-home); }
  .team.away .score { color: var(--gb-score-away); }

  .clock {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-inline: var(--space-1);
  }

  .time {
    font-family: var(--font-mono);
    font-weight: 800;
    font-size: clamp(1.5rem, 6vw, 2.4rem);
    color: var(--gb-clock);
    font-variant-numeric: tabular-nums;
    text-shadow: 0 0 14px rgba(255, 210, 74, 0.45);
  }

  .label {
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    color: #9fb0bf;
    margin-top: 0.2rem;
  }
</style>
